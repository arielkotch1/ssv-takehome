import { Text, Flex } from "rebass/styled-components"

export const Placeholder = () => {
  return (
    <Flex justifyContent={"center"} alignItems={"center"} minHeight={150}>
      <Text>No Results Found</Text>
    </Flex>
  )
}
